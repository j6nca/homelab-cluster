apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app csi-driver-nfs
  namespace: flux-system
spec:
  targetNamespace: kube-system
  commonMetadata:
    labels:
      service: *app
  interval: 5m
  path: ./apps/base/csi-driver-nfs/release
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
