apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: gatus-endpoints
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: homelab
  folderRef: system
  configMapRef:
    name: gatus-endpoints-dashboard
    key: gatus_endpoints.json
