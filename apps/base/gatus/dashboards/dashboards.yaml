apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: Gatus Endpoints
  namespace: grafana-operator
spec:
  instanceSelector:
    matchLabels:
      dashboards: homelab
  folder: "Services"
  configMapRef: gatus-endpoints-dashboard
#   json: >
# {{ .Files.Get "../../../../dashboards/system/gatus_endpoints.json" | indent 2 }}
