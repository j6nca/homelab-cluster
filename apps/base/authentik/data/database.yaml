apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: authentik-postgres
spec:
  instances: 3
  managed:
    roles:
      - name: authentik
        login: true
        passwordSecret:
          name: authentik-postgres
  storage:
    storageClass: longhorn
    size: 1Gi
