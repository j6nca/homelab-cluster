apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: authentik-postgres
spec:
  instances: 3
  managed:
    roles:
      - name: authentik
        passwordSecret:
          name: authentik-postgres
          key: password
  storage:
    storageClass: longhorn
    size: 1Gi
