# yaml-language-server: $schema=https://ctrl-research.github.io/k8s-schemas/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app kube-cats
spec:
  commonMetadata: &commonlabels
    labels:
      service: *app
      owner: j6n
      level: '3'
      group: tools
  chart:
    spec:
      chart: *app
      sourceRef:
        kind: HelmRepository
        name: j6nca
        namespace: flux-system
      version: 1.0.9
  interval: 15m
