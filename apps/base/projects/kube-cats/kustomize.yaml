# yaml-language-server: $schema=https://ctrl-research.github.io/k8s-schemas/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: &app kube-cats
  namespace: flux-system
spec:
  targetNamespace: *app
  commonMetadata:
    labels:
      service: *app
  interval: 5m
  path: ./apps/base/projects/kube-cats/release
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  # patches:
  #   - patch: |
  #       apiVersion: builtin
  #       kind: LabelTransformer
  #       metadata:
  #         name: common-labels
  #       labels:
  #         service: kube-cats
  #         owner: j6n
  #         level: '3'
  #     target:
  #       kind: .*
