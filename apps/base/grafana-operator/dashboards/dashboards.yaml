apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: datasource-status
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: homelab
  folderRef: System
  configMapRef:
    name: datasource-status-dashboard
    key: datasource-status.json
---
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: home
  namespace: observability
spec:
  instanceSelector:
    matchLabels:
      dashboards: homelab
  # Using empty object for dashboard in root folder.
  folderRef: {}
  configMapRef:
    name: home-dashboard
    key: home.json
